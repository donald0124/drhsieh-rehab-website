---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection, type CollectionEntry } from 'astro:content';

// 隨機取得 3 篇文章推薦
const allPosts = await getCollection('blog');
const randomPosts = allPosts
  .sort(() => Math.random() - 0.5)
  .slice(0, 3);
---

<BaseLayout title="找不到頁面 - 謝明福復健科診所" description="很抱歉，您要查找的頁面不存在或已移動。">
  
  <section id="404-hero" style="
    background: linear-gradient(135deg, var(--color-primary-light) 0%, #ffffff 100%);
    padding: 1rem 1rem 1rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  ">
    
    <!-- 裝飾性圖形 -->
    <div style="
      position: absolute;
      top: -50px;
      right: -50px;
      width: 200px;
      height: 200px;
      background: var(--color-primary);
      opacity: 0.05;
      border-radius: 50%;
    "></div>
    
    <div style="
      position: absolute;
      bottom: -30px;
      left: -30px;
      width: 150px;
      height: 150px;
      background: var(--color-primary);
      opacity: 0.05;
      border-radius: 50%;
    "></div>

    <div style="max-width: 700px; margin: 0 auto; position: relative; z-index: 1;">
      
      <!-- 圖標 -->
      <div style="
        font-size: 4rem;
        margin-bottom: 1.5rem;
      ">
        🔍
      </div>

      <!-- 標題 -->
      <h1 style="
        font-size: 1.5rem;
        color: var(--color-heading);
        margin-bottom: 1rem;
        font-weight: 600;
      ">
        很抱歉，找不到此頁面
      </h1>

      <!-- 說明文字 -->
      <p style="
        font-size: 1.1rem;
        color: var(--color-text);
        margin-bottom: 2rem;
        line-height: 1.7;
      ">
        您查找的文章可能已轉移或刪除。但別擔心，我們還有許多優質內容供您閱讀！
      </p>

      <!-- 按鈕區 -->
      <div style="
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 0.5rem;
      ">
        <a href="/" style="
          padding: 0.7rem 1.8rem;
          background-color: var(--color-primary);
          color: white;
          border-radius: 50px;
          text-decoration: none;
          font-weight: 600;
          transition: all 0.3s;
          box-shadow: 0 2px 8px rgba(0, 90, 156, 0.2);
        "
        onmouseover="this.style.backgroundColor='var(--color-primary-dark)'; this.style.transform='translateY(-2px)'; this.style.boxShadow='0 4px 12px rgba(0, 90, 156, 0.3)'"
        onmouseout="this.style.backgroundColor='var(--color-primary)'; this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 8px rgba(0, 90, 156, 0.2)'"
        >
          回到首頁
        </a>
        <a href="/blog/" style="
          padding: 0.7rem 1.8rem;
          background-color: white;
          color: var(--color-primary);
          border: 2px solid var(--color-primary);
          border-radius: 50px;
          text-decoration: none;
          font-weight: 600;
          transition: all 0.3s;
        "
        onmouseover="this.style.backgroundColor='var(--color-primary)'; this.style.color='white'"
        onmouseout="this.style.backgroundColor='white'; this.style.color='var(--color-primary)'"
        >
          所有文章
        </a>
      </div>

    </div>
  </section>

  <!-- 推薦文章區塊 -->
  <section id="recommended-articles" style="
    padding: 1.5rem 1rem 4rem;
    background-color: #ffffff;
  ">
    <div style="max-width: 1000px; margin: 0 auto;">
      
      <h2 style="
        text-align: center;
        font-size: 1.75rem;
        color: var(--color-heading);
        margin-bottom: 1.5rem;
        font-weight: 600;
      ">
        推薦閱讀
      </h2>

      <div style="
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
      ">
        {randomPosts.map((post: CollectionEntry<'blog'>) => (
          <a 
            href={`/blog/${post.slug}/`}
            style="
              text-decoration: none;
              background: white;
              border: 1px solid var(--color-border);
              border-radius: 8px;
              overflow: hidden;
              box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
              transition: all 0.3s ease;
              display: flex;
              flex-direction: column;
            "
            onmouseover="this.style.transform='translateY(-5px)'; this.style.boxShadow='0 8px 20px rgba(0, 0, 0, 0.1)';"
            onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 8px rgba(0, 0, 0, 0.05)';"
          >
            {post.data.heroImage && (
              <img 
                src={post.data.heroImage} 
                alt={post.data.title}
                style="
                  width: 100%;
                  height: 180px;
                  object-fit: cover;
                "
              />
            )}
            <div style="padding: 1.5rem; flex: 1; display: flex; flex-direction: column;">
              <h3 style="
                font-size: 1.2rem;
                color: var(--color-primary);
                margin: 0 0 0.75rem;
                line-height: 1.4;
              ">
                {post.data.title}
              </h3>
              <p style="
                font-size: 0.95rem;
                color: var(--color-text);
                margin: 0 0 1rem;
                flex: 1;
                line-height: 1.6;
              ">
                {post.data.description}
              </p>
              <p style="
                font-size: 0.85rem;
                color: #999;
                margin: 0;
              ">
                {post.data.pubDate.toLocaleDateString('zh-TW', { 
                  year: 'numeric', 
                  month: 'long', 
                  day: 'numeric' 
                })}
              </p>
            </div>
          </a>
        ))}
      </div>

    </div>
  </section>

  <!-- 聯絡資訊 -->
  <section style="
    padding: 3rem 1rem;
    background-color: var(--color-primary-light);
    text-align: center;
  ">
    <div style="max-width: 700px; margin: 0 auto;">
      <h3 style="
        font-size: 1.5rem;
        color: var(--color-heading);
        margin-bottom: 1rem;
      ">
        需要協助？
      </h3>
      <p style="
        font-size: 1.1rem;
        color: var(--color-text);
        margin-bottom: 1.5rem;
      ">
        歡迎直接與我們聯繫
      </p>
      <div style="
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
      ">
        <a href="tel:0226718910" style="
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          padding: 0.8rem 1.5rem;
          background-color: var(--color-primary);
          color: white;
          border-radius: 50px;
          text-decoration: none;
          font-weight: 600;
          transition: background-color 0.3s;
        "
        onmouseover="this.style.backgroundColor='var(--color-primary-dark)'"
        onmouseout="this.style.backgroundColor='var(--color-primary)'"
        >
          📞 (02) 2671-8910
        </a>
        <a href="https://line.me/R/ti/p/@gsj9393s" target="_blank" style="
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          padding: 0.8rem 1.5rem;
          background-color: #00B900;
          color: white;
          border-radius: 50px;
          text-decoration: none;
          font-weight: 600;
          transition: background-color 0.3s;
        "
        onmouseover="this.style.backgroundColor='#009700'"
        onmouseout="this.style.backgroundColor='#00B900'"
        >
          💬 LINE 諮詢
        </a>
      </div>
    </div>
  </section>

</BaseLayout>
